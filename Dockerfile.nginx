FROM nginx:alpine
RUN apk add --no-cache openssl || (apt-get update && apt-get install -y openssl)
RUN mkdir -p /etc/nginx/certs && openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout /etc/nginx/certs/local.key -out /etc/nginx/certs/local.crt -subj "/CN=localhost"